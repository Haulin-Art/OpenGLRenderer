- Communicates in Chinese and asks questions in Chinese; prefers responses in Chinese. Confidence: 0.8
- Sometimes wants step-by-step instructions to follow manually, but is equally comfortable delegating hands-on execution to the agent (asked it to download and install library files and edit `.gitignore` itself). Prefer offering both options rather than assuming. Confidence: 0.5
- Comfortable with the agent running shell/build commands, downloading external artifacts, and editing files on his machine end-to-end. Confidence: 0.7
- Does C/C++ development on Windows with the w64devkit MinGW-w64 toolchain, CMake + Ninja, and VS Code build tasks. Confidence: 0.7
- Expects the agent to re-read the latest on-disk contents before asserting what code contains; will push back (e.g. "不对呀，你现在看看") when an answer is based on stale/outdated file reads. Confidence: 0.6
- Distinguishes "explain it to me" requests from "do it for me" requests, and will explicitly say when he does NOT want code changed (e.g. "讲一下，不用改代码" / "你给我讲讲就可以，不用改文件" / "不要该我文件"); when asked to explain, wants an accurate walkthrough of the current code and suggested changes described, not applied. He often writes a change himself, then asks the agent to judge whether it actually works ("这个能用吗") — but expects the verdict to be backed by actually building/compiling his current on-disk code (not reasoning from the source alone), and to be delivered WITHOUT the agent editing his files. He also re-asks for explanation after having written code himself (e.g. "我新建了一个Camera类，按照我这个该怎么实现，讲一讲"), expecting a review + how-to rather than a patch. Confidence: 0.7
- When asking how to implement a concept, asks for thorough, in-depth explanations ("详细讲讲") — wants the underlying reasoning, ordering/rules, and common pitfalls, plus how it maps onto his existing code, not a minimal answer. He also asks these conceptual "how does X work / how do I structure X" questions about engine architecture (e.g. multi-pass rendering, render-to-texture, shader/mesh management for many objects) and expects the answer grounded in his actual files, with concrete code snippets and the traps to avoid. Confidence: 0.7
- Likes new functionality delivered as a separate, self-contained file (e.g. a standalone OBJ loader) with console output to inspect/verify the data, rather than folded into existing code; explicitly keeps refactors of his own classes (e.g. "我的 Mesh 类得改一下") for himself to do later. He will hand-edit his own core classes/assets (e.g. 改好 Mesh, 放入 obj 模型) and then delegate only the small glue change (e.g. "你在 main 当中替换一下") to the agent. Confidence: 0.65
- Likes to learn one concept at a time: when a topic feels too broad ("我没太搞明白"), he narrows the scope himself ("我们先从多物体多shader开始") and asks to have that single topic explained thoroughly before implementation; prefers a focused, incremental walkthrough over a big combined answer. Confidence: 0.55
- Self-describes as not having a strong C++ foundation ("我C++基础不是特别厚"); when an explanation feels too complex or too rushed ("有点太复杂我有点难以理解了", "你刚才讲得我有点没听明白，再讲的详细点"), he asks to have it re-taught in plain language, numbered micro-steps, from first principles (e.g. explaining what a pointer or a callback actually is) rather than with advanced C++ vocabulary (`unique_ptr`, Rule of Three). Prefers progressive disclosure: understand/land the minimal version first, then layer on complexity. He also pastes a concrete snippet (often one the agent proposed, before he hand-types it) and asks "讲一讲", expecting a line-by-line walkthrough of the C++ language mechanics it relies on — lambda/capture-list semantics, `std::tuple` lexicographic comparison as multi-level sort priority, the strict-weak-ordering contract of `std::sort`, converting a pointer to `uintptr_t` instead of comparing pointers, using a monotonic transform (comparing squared distances) to skip `sqrt` — each paired with the naive alternative and why it is wrong. Confidence: 0.7
- Wants advice fitted to the structure he has already designed himself ("按照我这个"), not a replacement architecture: when he has sketched his own class (e.g. a `Camera` class), the expected answer is a review of his version plus the minimal changes to make his design work, rather than the agent's preferred design. Confidence: 0.6
- Typically follows an "explain, then implement" rhythm on his own code: after a review of his class, he hands the concrete changes back to the agent to apply for him ("那你先帮我改一下Camera类吧"), then moves on to the next piece step by step — so the review should end with specific, ready-to-apply edits. Confidence: 0.55
- Opens design turns by demanding the plan/rationale first, even when implementation is the obvious eventual goal ("先说是怎么解耦 ... 讲一讲"): explain the concept and the options as a description, not as applied code, and let him react before anything lands. He usually pairs the request with his own objection to the recommendation ("我现在main里面还是包含OpenGLShader之类的，不然创建不出来") and expects the explanation to address that objection head-on — what is and is not actually wrong, what is unavoidable, and the trade-offs of each option — rather than just restating the refactor. Confidence: 0.6
- When he does want changes applied, he scopes the authorization to one specific named file (e.g. "你帮我改一下CMakeList") and in the same breath keeps everything else off-limits ("不可以的话跟我讲一讲，不要改") — expects the agent to touch only that file and then just report on the rest. Build/config edits (CMakeLists.txt) tend to get delegated to the agent, while his own source files stay his to hand-edit; he then explicitly reserves the rest for himself to review ("其它的我先自己看看") and expects the agent to stop there and simply report what remains untouched. The same scoping applies to additive work: he may ask for new components to be created as brand-new files and explicitly forbid editing any other file ("不要改其他文件，只是创建两个pass文件"), even when the change would normally require wiring — in that case he still expects the needed integration/assembly code to be spelled out as a description (not applied) so the existing-file edits stay his to do. Confidence: 0.65
- After applying his own round of fixes, he opens the next turn with a broad, often extremely terse "check it now" prompt ("现在看一下怎么样子，还有没有什么问题" / "现在呢，你看一下"), expecting the agent to infer the intent without further specification: re-read the current on-disk files, actually rebuild and run the code (including exercising the real program's lifecycle, e.g. triggering a graceful window-close/shutdown), then proactively enumerate remaining and newly-introduced pitfalls ranked by severity with a concrete fix each — rather than only judging the specific change he just made (e.g. after implementing a `RenderQueue` class himself as the agent described, he opened the next turn with just "OK，你看看现在的"). Confidence: 0.7
- Edits files in VS Code with auto-save off (`"files.autoSave": "off"`), so file state can drift in either direction: he may say he changed a file ("我改了Mesh类跟shader") while the on-disk version is still stale, and conversely the agent's disk writes to a file he has open can be silently reverted when he later hits Ctrl+S (he noticed "你改了么？我本地没变呀" after a successful write+build). Verify actual on-disk contents/mtimes and git status before asserting what a file contains or that an edit landed, and ask him to save — or close/revert the file's editor tab — before trusting either side. He may also point the agent at a file he just wrote but has not saved yet (e.g. "等下，刚刚忘了保存了，你再看一下"), so it reads empty/stale; when that happens, report what you actually see, ask him to save, and re-read rather than answering from the empty read. Confidence: 0.7
- Reports failures as a single terse question in Chinese with no repro steps or hypothesis — whether a runtime/rendering regression (e.g. "为什么现在什么都绘制不出来了") or a build/link failure (e.g. "现在为什么生成失败了，你看看") — delegating the whole diagnosis to the agent: expects it to read the current on-disk code and actually reproduce/inspect (rebuild, run the program, use a debugger/screenshot, count calls) to locate the true root cause and explain why, then propose concretely-scoped fixes — rather than guess from the source alone. This includes the case where he attempted a refactor/integration himself and it no longer builds ("我试图把创建这些资源整合到对应的Renderer，但是报错了"): expects the agent to actually compile his on-disk code to capture the errors and trace them back to the single root-cause signature (the compiler-noise errors that mislead should be flagged as such), plus a re-ordered, step-by-step 思路/walkthrough of the fix sequence — not just a patch. Same pattern when he adds a new object to the scene himself and reports the raw visual symptom in a single question (e.g. "猴头确实感觉有正确的阴影了，但是我又加了一个平面，为什么它是纯黑的"), pairing a confirmation that a previous fix worked with the new anomaly. Confidence: 0.65
- Keeps his questions/thoughts and design intent as markdown files inside the repo (e.g. `src/Core/想法.md` for questions, `src/目标渲染架构.md` for the target architecture doc) and uses them as a communication channel: he points the agent at the files ("你看一下，回答一下") expecting it to read them, answer the questions in chat, and also update/edit the architecture doc itself. Confidence: 0.5
- Wants repository documentation as thorough markdown in the repo itself: asked for a README that lists "当前有效文件" with each file's purpose, the architecture/layering, the relationships between files, and explicitly asked for it to be 详尽的 (exhaustive) — a file-by-file inventory with interfaces, dependencies, ownership/data-flow diagrams, and the known issues/TODO list, grounded in the real on-disk code rather than a summary. Treats the README as a living document: after later rounds of code changes he asks to sync it ("很好，完整看看现在的项目，更新readme"), expecting a full re-read of the current project (and re-verification/build/run to confirm its real state) before rewriting the affected sections, keeping the exhaustive structure rather than bolting on a changelog. Confidence: 0.65
- Favors proper encapsulation and layering in his engine: when offered a quick-but-dirty route (global pointer, main directly calling GLFW) versus a properly encapsulated one, he explicitly opts for the encapsulated design — wants platform-specific details (e.g. GLFW) hidden behind the renderer interface so high-level code (`main`) stays free of them ("我希望封装进OpenGLRenderer内该怎么做"). Confidence: 0.55
- Actively refactors toward an API-agnostic core by splitting tightly-coupled OpenGL classes into abstract interfaces plus backend-specific implementations (e.g. `IShader`/`IMesh` interfaces with `OpenGLShader`/`OpenGLMesh` implementations), explicitly to "解耦" the core layer from OpenGL. When reviewing such a refactor, he wants the interface/implementation mismatches, leftover old-type includes, and "新旧两套并存" pseudo-compiling states called out. Confidence: 0.55
- Iterates on decoupling one concrete type at a time: after each round of his own edits he asks whether the next remaining backend type can also be removed from the application layer (e.g. "去除了OpenGLShader跟OpenGLMesh，但是OpenGLRenderer有办法去除吗"). Expects an honest boundary answer — what is fundamentally unavoidable (the composition root must exist somewhere) vs. what can be reduced — together with the minimal-diff way to do it, rather than a flat "no" or a wholesale redesign. Confidence: 0.5
- Hand-types the agent's suggested snippets into his own files rather than having them applied, so the answer needs to be transcription-proof: state the exact place/scope (free function at namespace scope outside the class vs. `virtual` member inside it) and the full signature, because he otherwise reproduces a snippet in the wrong form or place (wrote the free `CreateRenderer(width, height)` factory as a `virtual IRenderer* CreateRenderer()` member and called it with no arguments, then reported only "不行呀，按照你的第一种方法不行呀"; and later applied a build-system-level switch at source level — pasting `#define OPENGL_RENDERER` into `main.cpp` instead of adding it to `target_compile_definitions`, so the define was invisible to the other translation unit and the program crashed at startup with "为什么我加上宏之后就连窗口就不会出了"). Say explicitly whether a change belongs in a source file vs. the build system/CMake, and in which file/scope. He also refers back to the agent's options by ordinal ("第一种方法"), so keep multi-option answers numbered and self-contained. Confidence: 0.5
- Doesn't stop at "it works": once a fix/snippet he typed in compiles and runs ("真的可以了"), he asks why it works at the mechanism level ("我不理解，为什么会这样呀") and probes hypothetical variations to test his mental model (e.g. why a free function can produce a concrete `OpenGLRenderer`, and which one it would pick if several backends existed). Expect the explanation to open the hood on the underlying machinery (declaration vs. definition, symbol resolution at link time, vtables/polymorphism, compile-time vs. runtime choice) rather than a restatement of the code, and to answer the "what if there were more than one" question with the full ladder of options. He keeps probing these multi-backend hypotheticals across turns (e.g. asking whether the factory implementation has to be written once per backend). Confidence: 0.55
- Prioritizes a working minimal version over architectural purity when integrating a new feature: he deliberately embeds the new code inline in the existing path first ("我打算先直接嵌入写，这样能行之后再搞其他的"), accepting a temporary shortcut even where it conflicts with his own preference for encapsulated/layered abstractions, and only generalizes/refactors once it runs. Treat "get it working end-to-end first, clean up later" as his default sequencing. Confidence: 0.55
- Prefers eliminating the "万能头"/umbrella header (e.g. a `config.h` that pulls in everything) in favor of explicit per-file includes, and expects CMakeLists and dependent files (leftover `#include "config.h"`) to be cleaned up accordingly. Confidence: 0.5
`OpenGLRenderer.h` will never include `VulkanRenderer.h`), and expects the "which backend to instantiate" decision to live in one centralized, conditional assembly point (a dedicated factory file) that is the only place allowed to know all backends — not a `CreateRenderer` re-written inside every backend. When reviewing his factory setup, answer whether the factory is per-backend or shared, confirm the assembly point is separate from the backends, and flag any place where one backend would have to include another. Confidence: 0.5
- Periodically asks for a top-level verdict on the current architecture itself ("这个架构怎么样", "就目前而言，这个架构怎么样", or about a specific file he just wrote: "看看…这样的架构行不行"), separate from a bug hunt: expects an honest assessment of whether the design direction is right, what was actually achieved (verified against the real build/symbols/grep rather than asserted), and which structural weaknesses/隐患 still remain, ranked by severity. Wants the strengths and the flaws both called out plainly rather than just a change report, and when the design is his own he wants it reviewed as-is (what's right, what to fix) rather than replaced. Confidence: 0.55
- When a technical explanation still doesn't land, he comes back on the exact same snippet with "还是没看懂" / "还是没完全搞懂" and asks again about that one line — a thorough, correct previous answer is not enough. He decomposes his remaining confusion into several crisp sub-questions rather than re-asking the whole thing: the cost/count of internal operations (e.g. "排序一共迭代了几次，是总数*总数次？" — wants n·log n vs n² cleared up, i.e. the algorithmic cost model, not just correctness), whether a piece of the mechanism works the way he suspects (he states his own hypothesis and asks to have it confirmed/corrected, e.g. "lambda 返回 bool，sort 是靠这个 bool 自动调整顺序吗？"), and "这个类要怎么用" — a concrete, copy-pasteable integration walkthrough (which file to add, what to change in `main`, and confirmation that the untouched layers really stay untouched). Expect to re-teach it in a different register: open with a single plain-language sentence stating what the line does, then decompose it piece by piece and hand-trace it with concrete values (real numbers fed through the comparison/algorithm, showing the intermediate results and why the result is what it is), rather than restating the same mechanics at the same level. Confidence: 0.7
- On a state-check turn he pairs "现在正不正常" with an explicit "下一步干什么" (what's next): expects the status verdict to be followed by a concrete, dependency-ordered roadmap of the next implementation step(s) — numbered steps sequenced so prerequisites come first, each with the trap it avoids flagged explicitly (e.g. unify the light source before adding shadow-map sampling, or you can't tell direction from sampling bugs; render/visualize the intermediate buffer before writing the comparison logic, or "no effect" and "wrong math" look identical). Not just a list of what's broken, but the ordered plan of what to build next. Confidence: 0.5
- Does his own controlled bisection before asking: he isolates a cause with a minimal A/B toggle in his code (e.g. commenting out the `Submit(...)` line that adds the new plane) and reports both discriminated outcomes in one breath ("经过我的测试 ... 129行注释掉 ... 就正常"), expecting the agent to treat that experiment as a firm data point, reproduce it, and explain the mechanism behind the difference rather than re-derive it from scratch. Confidence: 0.5
- When a part of his renderer becomes unwieldy (e.g. "要创建很多 Pass" bloating `OpenGLRenderer`), he proactively proposes introducing a dedicated interface/base class for that concept (a "RendererFeature"/Pass base class composed as an ordered list inside the Renderer) instead of piling on hardcoded code, and asks for the arrangement to be grounded in how modern engines do it (e.g. Unity URP). He is fine with those Pass classes directly containing backend-specific/low-level code (calling OpenGL commands) — pragmatic about the abstraction boundary rather than forcing a premature fully-backend-agnostic layer. Confidence: 0.55
- Prefers to develop a new subsystem iteratively and in isolation before wiring it into the real program flow: he asks the agent to "试着写一下，然后迭代迭代" (write a first version, then iterate on it), and to hold off integrating it yet ("暂时不先入实际的程序流程"). Concretely, the new code should be built against the real codebase so that it compiles, and the existing program should still build and run with its behavior provably unchanged (acceptance = current rendering stays identical) while nothing actually instantiates the new code — integrate only after that. Confidence: 0.6
- Explicitly grants the agent authority to refactor/change foundational base classes when the plan calls for it ("你是可以更改调整基类的"), in contrast with turns where he scopes authorization to a single named file; so don't assume base classes are always off-limits — when he opens that door, make the base-class change and call out clearly what was changed and why. Confidence: 0.5
- When integrating a verified subsystem, wants it to fully REPLACE the superseded code path, not coexist with it: after the isolated Pass work was validated he said "接入主程序，替代进去", expecting the old inline implementation and now-dead members/functions (the inline shadow/base rendering, `InitShadowPass()`, helpers that moved into the new classes) to be deleted rather than left alongside the new list. Treat "接入/替代进去" as: swap the new route in end-to-end and remove the old one, while keeping any unrelated scaffolding he is separately building intact. Confidence: 0.5
- A short post-plan go-ahead ("很好，把这套接入主程序") is broad authorization: it unlocks multi-file rewrites and deletions (rewriting `OpenGLRenderer.h/.cpp`, removing members/functions, editing CMake), the opposite of his file-scoped "只改这一个文件" turns — so once he has approved a plan, execute the full change rather than asking per-file. Confidence: 0.5
- Evaluates rendering/intermediate results for physical and geometric plausibility, not just whether an effect is present: he will call out a result that is technically applied but spatially unreasonable (e.g. SSAO still obvious at long distances, a dark ring/halo forming around an object) and frames the complaint as "you only implemented how to produce the effect, but not whether the effect is reasonable" ("你似乎只考虑了ao的产生，没考虑到ao是否合理"). Expects such fixes to add a physically-motivated weighting/fade (distance checks, range checks, distance-based falloff) rather than arbitrarily retuning constants to mask the artifact. Confidence: 0.55
- Builds the pass pipeline by issuing terse, goal-level implement commands that name the subsystem and the exact data it must output (e.g. "实现基础GBuffer Pass，输出世界法线以及世界深度纹理"), while leaving every implementation decision to the agent — attachment formats, MRT layout, the shaders, resize handling. He thinks in terms of each pass's declared data contract (inputs/outputs) rather than prescribing internals. This unscoped "实现 X" phrasing is broad authorization: create the new files AND wire them into the surrounding infrastructure (frame-context fields, stage enum, renderer assembly, CMake) without asking per-file — the opposite of turns where he explicitly scopes edits to one named file. Confidence: 0.5
- New rendering effects should be runtime-toggleable, not always-on: when requesting a new pass he asks for it to have "可以选择是否开启" (a switch to turn it on/off at runtime), so he can enable/disable it and A/B compare the effect with and without it. Expect the effect to be structured so that toggling it off leaves the picture pixel-identical to before the feature existed (a clean baseline), with the switch surfaced to him (a key/console message). Confidence: 0.45
- Insists that a per-pixel expensive effect (e.g. PCSS shadowing) must not be duplicated inside every material shader — calls that arrangement "不合理" — and prescribes the correct screen-space pipeline himself: compute it once into a screen-space texture (may be downscaled for cost), then denoise it with a separate bilateral/blur pass, then let the material do a single texture sample. He treats the SSAO structure (compute → blur pass → sample in material) as the reference pattern and expects new heavy effects (shadows) to be restructured the same way, with the material shader slimmed down to just sampling. Confidence: 0.6
