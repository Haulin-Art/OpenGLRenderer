# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll
*.so.*


# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# Build directories
build/
Build/
build-*/

# CMake generated files
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
install_manifest.txt
compile_commands.json

# Temporary files
*.tmp
*.log
*.bak
*.swp

# vcpkg
vcpkg_installed/

# debug information files
*.dwo

# test output & cache
Testing/
.cache/

# ============================================================
# 例外区
#
# 注意: .gitignore 是「后面的规则覆盖前面的」，所以例外必须写在最后。
#       另外 '!' 无法重新包含「父目录已被排除」的文件，
#       这里 src/ 和 src/mesh/ 都没有被排除，所以下面的写法有效。
# ============================================================

# 1) 第三方预编译库：上面的 *.a / *.lib 会误伤随仓库分发的第三方静态库
#    这里对 GLFW 的预编译库目录放行，保证换台电脑 clone 后仍能编译
!dependencies/glfw/lib-*/
!dependencies/glfw/lib-*/**

# 2) 3D 模型：上面的 *.obj 是给 MSVC 的目标文件用的，
#    但 Wavefront 的模型文件也是 .obj，会被一并忽略（.mtl 不受影响）
#    → 对模型目录放行，否则 clone 下来缺模型、程序跑不起来
#    以后新增模型目录时，在这里照抄一行即可
!src/mesh/*.obj
